{
    "scripts": {
        "build": "JEKYLL_ENV=production bundle exec jekyll build",
        "now": "npm run build && cd ~/projects/cabanon/_site && now --public",
        "optim": "cd ~/projects/cabanon/img && imageoptim --jpegmini '**/*.jpg' '**/*.jpeg'",
        "start": "bundle exec jekyll serve"
    }
}